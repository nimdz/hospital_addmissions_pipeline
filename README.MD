# Readmission prediction pipeline (synthetic data)

## Setup
Create a virtualenv / conda env and install dependencies:
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

